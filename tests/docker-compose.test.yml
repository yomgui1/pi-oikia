services:
  homeassistant:
    image: homeassistant/home-assistant:stable
    container_name: pi-ha-test
    volumes:
      - ./test-config:/config
    ports:
      - "8123:8123"
    environment:
      - TZ=UTC
    restart: unless-stopped
